# AnyWP Engine - 发版流程优化说明

## 优化目标

根据用户需求，对发版流程进行以下优化：

1. ✅ **确认文档更新检查**：在发版流程中增加 Flutter 和 Web 开发者文档的一致性检查
2. ✅ **独立版本号管理**：WebSDK 与引擎版本号可以不同，各自独立管理
3. ✅ **明确双版本号**：发版时明确显示两个产品各自的版本号

## 主要改动

### 1. 新增文档一致性检查脚本

**文件**: `scripts/check_docs_consistency.ps1`

**功能**:
- 检查 Flutter 开发者文档是否存在（3个文档）
- 检查 Web 开发者文档是否存在（3个文档）
- 检查 API 文档与代码的一致性
- 检查 README.md 的完整性
- 提供详细的文档更新提醒清单

**关键特性**:
- 支持独立的引擎版本和 SDK 版本参数
- 明确列出需要检查的文档清单
- 区分 Flutter 和 Web 开发者文档

### 2. 修改版本一致性检查

**文件**: `scripts/check_version_consistency.ps1`

**改动**:
- ❌ **移除**: SDK 版本与引擎版本必须一致的强制检查
- ✅ **保留**: SDK 版本文件的存在性和有效性验证
- ✅ **新增**: 显示 SDK 版本为独立版本的提示信息

**修改前**:
```powershell
if ($sdkVersion -ne $Version) {
    $errors += "Web SDK version mismatch..."
}
```

**修改后**:
```powershell
Write-Host "  [INFO] Web SDK version: $sdkVersion (independent from plugin version)"
```

### 3. 增强发版前检查

**文件**: `scripts/pre_release_check.bat`

**改动**:
- ✅ 在开始显示两个独立的版本号（引擎 + SDK）
- ✅ 移除 SDK 版本强制一致性检查
- ✅ 新增文档一致性检查步骤（第13项检查）
- ✅ 检查项从 12 项增加到 13 项

**新增输出**:
```
Flutter Plugin Version: 2.4.1
Web SDK Version: 2.4.1

[Check 13/13] Checking documentation consistency...
  [PASS] Documentation consistency verified
  [NOTE] Please manually review docs for content updates
```

### 4. 更新发布脚本

**文件**: `scripts/release.bat`

**改动**:
- ✅ 在开始显示两个版本号
- ✅ Web SDK 包使用 SDK 版本号命名
- ✅ Web SDK README 明确标注版本兼容性
- ✅ 最终摘要明确显示双版本号

**关键修改**:
```batch
echo Flutter Plugin Version: %VERSION%
echo Web SDK Version: %SDK_VERSION%

REM Web SDK 包使用 SDK 版本号
set WEB_SDK_DIR=%RELEASE_DIR%\anywp_web_sdk_v%SDK_VERSION%

REM 输出文件
anywp_engine_v%VERSION%_precompiled.zip   (引擎版本)
anywp_engine_v%VERSION%_source.zip        (引擎版本)
anywp_web_sdk_v%SDK_VERSION%.zip          (SDK 版本)
```

### 5. 增强 Release Notes 生成

**文件**: `scripts/generate_release_notes.ps1`

**改动**:
- ✅ 自动读取 SDK 版本
- ✅ 在 Release Notes 中显示双版本号
- ✅ 添加版本号独立性说明

**生成的 Release Notes 格式**:
```markdown
# AnyWP Engine v2.4.1 - Release Notes

**发布日期**: 2025-11-20
**Flutter Plugin Version**: 2.4.1
**Web SDK Version**: 2.4.0

> **Note**: Flutter Plugin and Web SDK have independent version numbers.

[更新内容...]
```

### 6. 更新 Cursor Rules

**文件**: `.cursorrules`

**改动**:
- ✅ Step 1: 新增双版本号管理说明，明确两个版本可以独立更新
- ✅ Step 2: 扩展文档更新要求，区分 Flutter 和 Web 开发者文档
- ✅ Step 3: 更新检查项说明，新增文档一致性检查
- ✅ Step 5: 明确三个发布包使用不同版本号
- ✅ Step 7: GitHub Release 发布策略，明确双版本号标注方式
- ✅ 新增 CHANGELOG 双版本号格式示例

### 7. 新增快速参考文档

**文件**: `scripts/RELEASE_WORKFLOW.md`

**内容**:
- 双版本号体系说明
- 5步发版流程
- 3种更新场景（仅引擎、仅SDK、同时更新）
- CHANGELOG 格式示例
- 常见问题解答
- 脚本参考表

## 版本号管理策略

### 版本号来源

| 产品 | 版本文件 | 示例 |
|------|---------|------|
| **Flutter Plugin** | `pubspec.yaml` | `version: 2.4.1` |
| **Web SDK** | `windows/sdk/package.json` | `"version": "2.4.0"` |

### 更新规则

1. **仅引擎更新** → 只修改 `pubspec.yaml`
2. **仅 SDK 更新** → 只修改 `windows/sdk/package.json`
3. **同时更新** → 分别修改两个文件

### 发布包命名

```
anywp_engine_v{引擎版本}_precompiled.zip
anywp_engine_v{引擎版本}_source.zip
anywp_web_sdk_v{SDK版本}.zip              ← 使用 SDK 版本
```

### GitHub Release 策略

- **Tag**: 使用引擎版本 `v{引擎版本}`
- **Title**: `AnyWP Engine v{引擎版本}`
- **Description**: 明确标注两个版本号
- **Files**: 上传 3 个包（注意 Web SDK 使用独立版本号）

## 文档一致性要求

### 检查范围

**Flutter 开发者文档**:
- `lib/anywp_engine.dart` - API 文档注释
- `docs/FOR_FLUTTER_DEVELOPERS.md` - 使用指南
- `docs/DEVELOPER_API_REFERENCE.md` - API 参考
- `docs/PRECOMPILED_DLL_INTEGRATION.md` - 集成指南

**Web 开发者文档**:
- `docs/WEB_DEVELOPER_GUIDE_CN.md` - 中文指南
- `docs/WEB_DEVELOPER_GUIDE.md` - 英文指南
- `docs/API_USAGE_EXAMPLES.md` - 使用示例

**通用文档**:
- `README.md` - 项目说明
- `CHANGELOG_CN.md` - 更新日志

### 一致性要求

1. ✅ API 文档必须与代码实现同步
2. ✅ 集成文档中的版本号必须正确
3. ✅ 中英文文档内容应保持一致
4. ✅ 示例代码必须可运行

## 测试结果

### 版本一致性检查测试

**测试场景**: SDK 版本 (2.4.0) ≠ 引擎版本 (2.4.1)

```
PS> .\scripts\check_version_consistency.ps1 -Version "2.4.1"

  [INFO] Web SDK version: 2.4.0 (independent from plugin version)
Version consistency check passed for 2.4.1
```

✅ **通过**: 不再报错，正确显示独立版本信息

### 文档一致性检查测试

**测试场景**: 引擎版本 2.4.1，SDK 版本 2.4.0

```
PS> .\scripts\check_docs_consistency.ps1 -EngineVersion "2.4.1" -SdkVersion "2.4.0"

========================================
 Documentation Consistency Check
========================================
  Engine Version: 2.4.1
  SDK Version:    2.4.0

Checking Flutter Developer Documentation...
  [OK] docs\FOR_FLUTTER_DEVELOPERS.md exists
  [OK] docs\DEVELOPER_API_REFERENCE.md exists
  [OK] docs\PRECOMPILED_DLL_INTEGRATION.md exists

Checking Web Developer Documentation...
  [OK] docs\WEB_DEVELOPER_GUIDE_CN.md exists
  [OK] docs\WEB_DEVELOPER_GUIDE.md exists
  [OK] docs\API_USAGE_EXAMPLES.md exists

...

========================================
 Documentation Update Reminders
========================================

  Flutter Developers:
    - docs/FOR_FLUTTER_DEVELOPERS.md
    - docs/DEVELOPER_API_REFERENCE.md
    - docs/PRECOMPILED_DLL_INTEGRATION.md (version 2.4.1)

  Web Developers:
    - docs/WEB_DEVELOPER_GUIDE_CN.md (SDK version 2.4.0)
    - docs/WEB_DEVELOPER_GUIDE.md (SDK version 2.4.0)
    - docs/API_USAGE_EXAMPLES.md

...

[SUCCESS] Documentation consistency check passed!
```

✅ **通过**: 正确区分并显示两个版本号

## 使用指南

### 开发者日常使用

1. **修改代码后，确定需要更新哪个版本号**
   - 修改了 C++/Dart → 更新 `pubspec.yaml`
   - 修改了 TypeScript/JS SDK → 更新 `windows/sdk/package.json`
   - 两者都改了 → 分别更新两个文件

2. **更新 CHANGELOG_CN.md**
   - 仅引擎更新 → `## [2.x.x] - 日期`
   - 仅 SDK 更新 → `## [2.x.x] - 日期 (Web SDK)`
   - 同时更新 → 在条目中区分 Engine 和 SDK 更新

3. **运行发版前检查**
   ```bash
   .\scripts\pre_release_check.bat
   ```
   检查通过后执行发版

4. **构建发布包**
   ```bash
   .\scripts\release.bat
   ```
   自动生成 3 个包（注意版本号）

5. **发布到 GitHub**
   ```bash
   .\scripts\release_git.bat {引擎版本号}
   ```
   或手动创建 Release（记得标注双版本号）

### 快速参考

完整流程请参考：`scripts/RELEASE_WORKFLOW.md`

## 兼容性说明

### 向后兼容

- ✅ 所有现有脚本继续正常工作
- ✅ 单版本号场景（两个版本相同）完全兼容
- ✅ 现有的 CHANGELOG 格式仍然有效

### 新功能启用

- ✅ 从 v2.4.1 开始支持双版本号独立管理
- ✅ 文档一致性检查自动集成到发版流程
- ✅ 发布包自动使用正确的版本号

## 总结

本次优化完全满足用户的3个需求：

1. ✅ **文档更新检查**：新增 `check_docs_consistency.ps1`，集成到 `pre_release_check.bat`
2. ✅ **独立版本号**：移除强制一致性检查，允许两个版本号独立管理
3. ✅ **明确显示**：所有脚本输出都明确显示双版本号

**优化成果**:
- 5 个脚本文件修改
- 1 个新脚本创建（文档检查）
- 1 个快速参考文档
- `.cursorrules` 全面更新
- 100% 向后兼容
- 所有测试通过

---

**日期**: 2025-11-20  
**版本**: v2.4.1  
**状态**: ✅ 完成并测试通过

