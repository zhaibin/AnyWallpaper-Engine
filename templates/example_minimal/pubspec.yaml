name: anywp_engine_example_minimal
description: Minimal desktop example for AnyWP Engine precompiled package
publish_to: 'none'
version: 0.0.1+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  anywp_engine:
    path: ../anywp_engine_v__VERSION__

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

