# 脚本说明

本目录包含 AnyWP Engine 项目的所有构建和运行脚本。

## 📜 脚本列表

### 🔧 setup_webview2.bat
安装 WebView2 SDK 和 NuGet 工具。

**用途**: 首次构建前必须运行此脚本。

**功能**:
- 自动检测是否已安装 WebView2 SDK
- 下载 NuGet（如果不存在）
- 安装 Microsoft.Web.WebView2 包

**使用**:
```bash
.\scripts\setup_webview2.bat
```

---

### 🏗️ build_and_run.bat
构建并运行示例应用（推荐）。

**用途**: 一键构建和运行。

**功能**:
- 自动检查并安装 WebView2 SDK（如果需要）
- 构建 Debug 版本
- 自动启动应用程序

**使用**:
```bash
.\scripts\build_and_run.bat
```

---

### ▶️ run.bat
灵活的运行工具，支持多种模式。

**用途**: 运行已构建的应用或使用 Flutter 运行。

**选项**:
```bash
run.bat [选项]

选项:
  -d, --debug      运行 Debug 版本 (默认)
  -r, --release    运行 Release 版本
  -f, --flutter    使用 Flutter 运行 (支持热重载)
  -h, --help       显示帮助信息
```

**示例**:
```bash
# 运行 Debug 版本
.\scripts\run.bat

# 运行 Release 版本
.\scripts\run.bat --release

# 使用 Flutter 运行（开发模式）
.\scripts\run.bat --flutter

# 使用 Flutter 运行 Release
.\scripts\run.bat -r -f
```

---

### 🧪 test.bat
自动测试脚本，带详细说明和日志监控。

**用途**: 测试壁纸功能是否正常。

**功能**:
- 显示详细的测试说明
- 列出预期效果
- 提示关键日志监控点
- 自动选择可用版本（Release 优先）

**使用**:
```bash
.\scripts\test.bat
```

---

### 📤 PUSH_TO_GITHUB.bat
Git 推送辅助脚本。

**用途**: 快速提交并推送代码到 GitHub。

**使用**:
```bash
.\scripts\PUSH_TO_GITHUB.bat
```

---

## 🚀 快速开始流程

### 首次使用

```bash
# 1. 安装依赖
.\scripts\setup_webview2.bat

# 2. 构建并运行
.\scripts\build_and_run.bat
```

### 日常开发

```bash
# 开发模式（支持热重载）
.\scripts\run.bat -f

# 或直接运行已编译版本
.\scripts\run.bat
```

### 测试

```bash
# 运行自动测试
.\scripts\test.bat
```

---

## 📋 常见问题

### Q: 构建失败，提示找不到 WebView2.h
**A**: 运行 `.\scripts\setup_webview2.bat` 安装 SDK

### Q: 找不到可执行文件
**A**: 先运行 `.\scripts\build_and_run.bat` 构建项目

### Q: 如何使用热重载？
**A**: 使用 `.\scripts\run.bat -f` 以 Flutter 模式运行

---

## 🔗 相关文档

- [快速开始](../docs/QUICK_START.md)
- [测试指南](../docs/TESTING_GUIDE.md)
- [构建信息](../docs/BUILD_INFO.md)

